{
  "name": "typescriptcms-workspace",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:admin",
    "dev:admin": "npm run dev --workspace @typescriptcms/admin",
    "dev:example": "npm run dev --workspace @typescriptcms/example",
    "dev:library": "node -e \"console.log('@typescriptcms/library is plugin-only (no dev server).')\"",
    "conductor:setup": "./scripts/conductor-setup.zsh",
    "conductor:run": "./scripts/conductor-run.zsh",
    "build": "npm run build --workspace @typescriptcms/library && npm run build --workspace @typescriptcms/example && npm run build --workspace @typescriptcms/admin",
    "build:admin": "npm run build --workspace @typescriptcms/admin",
    "build:example": "npm run build --workspace @typescriptcms/example",
    "build:library": "npm run build --workspace @typescriptcms/library"
  }
}
