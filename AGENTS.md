# Repository Agent Rules

- NEVER hardcode preview-update behavior inside `packages/example`.
- Preview logic in the example must stay generic and demonstrative, so it reflects how admin preview mode and library output are intended to work in real projects.
